# Environment variables (missing actual .env file)
DATABASE_URL=postgresql://localhost/mydb
API_KEY=your_api_key_here
NEXT_PUBLIC_API_URL=http://localhost:3001

# Note: This is just an example file, not the actual .env
# The build will fail because required environment variables are missing